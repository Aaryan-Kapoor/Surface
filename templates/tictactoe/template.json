{
  "name": "tictactoe",
  "description": "Tic-tac-toe against the agent — the human's move arrives as an action, the agent's reply arrives as state",
  "params": {
    "you": {
      "type": "string",
      "default": "X",
      "description": "The human's mark"
    },
    "agent": {
      "type": "string",
      "default": "O",
      "description": "The agent's mark"
    },
    "tour_step": {
      "type": "string",
      "default": "",
      "description": "Tour only: the step indicator, e.g. \"2 of 7\". Empty hides the tour rail."
    },
    "tour_next": {
      "type": "string",
      "default": "",
      "description": "Tour only: label for the advance button"
    },
    "tour_id": {
      "type": "string",
      "default": "",
      "description": "Tour only: which step this is, echoed back on the next action"
    }
  },
  "state": {
    "board": {
      "type": "string",
      "default": "........."
    },
    "turn": {
      "type": "string",
      "default": "you"
    },
    "status": {
      "type": "string",
      "default": "playing"
    },
    "note": {
      "type": "string",
      "default": ""
    }
  },
  "actions": [
    "move",
    "restart",
    "next"
  ]
}
