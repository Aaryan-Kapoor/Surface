{
  "name": "tour",
  "description": "The tour's front door — the user picks how they want to try Surface, and the choice comes back as an action",
  "params": {
    "heading": {
      "type": "string",
      "default": "How do you want to try Surface?",
      "description": "The headline"
    },
    "subhead": {
      "type": "string",
      "default": "Pick one. Whatever you pick, I build it here and react to what you do in it.",
      "description": "One line under the headline"
    },
    "options": {
      "type": "list",
      "default": [],
      "description": "Cards: [{ id, title, blurb, proves }]. Falls back to the built-in six."
    }
  },
  "state": {
    "status": { "type": "string", "default": "open" },
    "picked": { "type": "string", "default": "" }
  },
  "actions": ["pick"]
}
