{
  "name": "surface",
  "version": "0.1.0",
  "description": "Surface — universal display for AI agents. Push HTML/CSS/JS to a PWA via the `surface` CLI; react to user clicks with `surface wait`. See SKILL.md."
}
